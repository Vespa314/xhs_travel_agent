### 1. opencli xiaohongshu search 命令
- **用法**：`opencli xiaohongshu search "关键词"`
- **返回格式**：YAML格式，包含rank、author、author_url、likes、title、url、published_at等字段
- **经验**：
  - 搜索结果最多返回约18条，关键词越精准结果越好
  - 建议用多种不同关键词多次搜索同一主题，以获取更全面的结果
  - 部分帖子的url中xsec_token参数很重要，后续访问note或download时需要完整url

### 2. opencli xiaohongshu note 命令
- **用法**：`opencli xiaohongshu note "完整url（含xsec_token）"`
- **关键注意**：url必须包含xsec_token参数，否则可能无法访问
- **返回格式**：YAML格式，包含title、author、content、likes、collects、comments、tags
- **经验**：
  - 很多攻略帖的**核心内容写在图片里**，文字content可能只有标签，必须下载图片查看
  - 部分帖子文字内容很丰富，两种情况都存在
  - 运行一段时间后可能出现 `BROWSER_CONNECT` 错误（浏览器连接断开），需要重新连接Chrome

### 3. opencli xiaohongshu download 命令
- **用法**：`opencli xiaohongshu download "完整url" --output 目录路径`
- **返回格式**：在指定目录下创建以note_id命名的子目录，图片命名为 `{note_id}_{序号}.jpg`
- **经验**：
  - 下载的图片不是直接放在指定目录，而是创建子目录存放
  - 部分帖子包含视频（.mp4），也会一起下载
  - 下载成功后会显示每个文件的类型（image/video）和大小

### 4. 图片分析（使用MCP工具）
- 使用 `mcp__zai-mcp-server__extract_text_from_screenshot` 工具分析下载的图片
- **注意**：必须使用完整的绝对路径（包含子目录），不能省略子目录层级
- OCR效果对于清晰的排版文字识别效果好，对于艺术体文字识别有一定困难
- 部分图片可能只是风景照，不包含文字信息
- `mcp__zai-mcp-server__analyze_image` 也可以分析本地图片文件，部分图片可能触发内容安全过滤

### 5. 遇到的问题

#### 问题1：BROWSER_CONNECT 错误
- **现象**：运行多条命令后，突然报错 `Browser Bridge extension not connected`
- **原因**：opencli依赖Chrome浏览器插件，长时间运行后连接可能断开
- **解决**：
  - 确保Chrome浏览器保持打开状态
  - 尝试 `opencli daemon stop && opencli doctor`
  - 重新启动Chrome或重新加载插件
- **建议**：在大量搜索前确保Chrome稳定运行，分批执行命令

#### 问题2：帖子内容为空（只有标签）
- **现象**：很多帖子返回的content字段只有话题标签，没有实际文字
- **原因**：小红书用户习惯将攻略信息写在图片中
- **解决**：必须使用download命令下载图片，再用OCR工具分析图片内容

#### 问题3：搜索结果的URL格式
- **现象**：搜索返回的url是 `search_result/xxx?xsec_token=xxx` 格式
- **注意**：这个url可以直接用于 `note` 和 `download` 命令，但必须保留完整的xsec_token参数
- **建议**：保存搜索结果时完整保存url，不要截断

#### 问题4：MCP图片分析工具连接断开
- **现象**：调用图片分析工具时偶尔返回 `MCP error -32000: Connection closed`
- **解决**：重试调用通常可以解决

### 6. 最佳实践总结
1. **搜索阶段**：用3-5种不同关键词搜索同一主题，获取全面结果
2. **筛选阶段**：优先阅读高赞帖子（>100赞），同时关注低赞但内容详实的帖子
3. **验证阶段**：同一信息至少2-3个帖子交叉确认，避免被单一广告帖误导
4. **图片下载**：对文字内容为空的帖子一定要下载图片分析
5. **批量操作**：注意Chrome连接稳定性，建议每5-10条命令后检查连接状态

### 7. 图片下载默认路径
- 不使用 `--output` 参数时，图片默认下载到项目根目录的 `xiaohongshu-downloads/<note_id>/` 下

### 8. 小红书笔记数据质量判断
- 高赞笔记（>1000赞）通常信息更可靠
- 注意区分广告笔记：如果笔记中反复推荐某个特定品牌/店铺且语言很像广告话术，可能是广告
- 官方文旅账号发布的笔记信息可靠但可能偏官方宣传

### 9. 关键词搜索策略
- 先用宽泛关键词获取整体信息
- 再用具体关键词深入搜索（如具体景点名+攻略/预约）
- 对于特定景点，单独搜索该景点名称+攻略/预约等词

### 10. 门票价格信息获取策略
- 游客实拍的景区售票窗口价格公示牌照片，是获取最新门票价格的好来源
- 官方景区账号发布的帖子信息最准确，但可能偏官方宣传
- 门票价格信息建议下载图片用OCR提取，因为文字content很少包含具体价格数字
- 同一景区不同入口票价可能差异巨大，需分别搜索确认

### 11. 多天行程规划时的搜索策略
- 先搜索"城市名+天数+旅游攻略"获取整体框架
- 按框架中的景点逐个深入搜索门票、注意事项
- 美食搜索用"城市名+美食推荐/必吃"等宽泛关键词，本地人推荐的帖子最可靠
- 避免过多并发请求小红书，每搜索3-5次后暂停一下，防止触发风控

### 12. 攻略信息的可信度交叉验证
- 景区门票价格：多个帖子交叉确认 + 官方售票窗口照片最可靠
- 美食推荐：本地人的帖子通常比游客帖更靠谱
- 住宿建议：多个帖子交叉推荐同一区域，可信度较高
- 注意区分"景区官方账号"和"旅游中介账号"，后者可能是广告

### 13. 高德地图 API 地理编码经验
- 有两种API可用于坐标查询：地理编码API（`geocode/geo`）和POI搜索API（`place/text`）
- **大型景区**用地理编码API可能返回的位置不够精确，应改用POI搜索API配合具体关键词获取景区入口/游客中心的精确坐标
- **餐厅等小商家**直接用POI搜索API效果更好，地理编码API往往查不到
- **交通枢纽**用地理编码API效果较好
- 高德REST API有速率限制，不要开太多并发agent同时请求，建议顺序执行或小批量（3-5个一批）
- **密钥不要硬编码在HTML中**：应将key和securityJsCode独立到`config.js`文件中（通过`window.__AMAP_CONFIG`暴露），HTML通过`<script src="config.js">`引入，并将`config.js`加入`.gitignore`

### 14. 高德地图 JSAPI v2.0 地图HTML构建经验
- **安全密钥配置**：必须在`AMapLoader.load()`之前设置`window._AMapSecurityConfig = { securityJsCode: '...' }`
- **埋点标识**：必须在`AMapLoader.load().then()`回调第一行设置`AMap.getConfig().appname = 'amap-jsapi-skill'`
- **自定义标记**：使用Marker的`content`属性传入HTML字符串，可以完全控制样式；配合`offset`属性调整锚点位置
- **InfoWindow**：设置`isCustom: true`后不显示默认边框，需自己实现关闭按钮和样式；通过`setContent()`和`open(map, position)`动态更新
- **驾车路线**：`AMap.Driving`插件的`search(origin, destination, {waypoints: [...]}, callback)`支持途经点；路线计算是异步的，需在回调中处理
- **驾车路线缓存到localStorage**：路线数据应缓存到`localStorage`，key按项目前缀+天数编号，value存`{waypoints, path}`。加载时优先读缓存，同时验证waypoints坐标是否与当前数据一致（防止数据变更后用了旧缓存）
- **路线视觉**：叠加一条较宽半透明的阴影线+一条较窄不透明的主线，视觉效果更好；设置`showDir: true`可显示方向箭头
- **覆盖物显隐**：Polyline和Marker都有`show()`/`hide()`方法，可用于路线的开关切换
- **地图样式**：`amap://styles/whitesmoke`（远山黛）适合旅游攻略类地图，视觉干净不喧宾夺主

### 15. 地图HTML中POI数据组织经验
- 按类型分组POI数据（景点、美食、交通），每条记录包含id、name、坐标、所属天数、描述、价格等字段
- 用天数(day)字段关联路线，day=0表示通用推荐（不绑定特定天）
- 美食店信息窗中附带小红书帖子链接，方便用户核实
- 住宿区域用Circle（虚线描边+半透明填充）标注推荐范围，比Polygon更直观
- 路线数据用waypoints数组定义每天的行程点顺序，起点→途经点→终点
- **分段驾车距离/时间必须预计算**：不要用Haversine直线距离估算！应在构建HTML前，用高德驾车路线REST API逐段查询真实驾车距离和耗时，将`dist`(km)和`time`(min)直接写入每个途经点的数据中

### 16. 地图HTML中覆盖物管理与UI设计经验
- **按天管理覆盖物显隐**：用`dayOverlays`对象统一管理所有Marker，筛选时只需遍历对应天数的数组调用`show()`/`hide()`
- **住宿区域标签位置**：Circle中心的文字标签容易与POI标记重叠，应将标签放在圆形顶部（纬度偏移半径对应的度数）
- **路线停靠点标记设计**：起点用带"起"字的圆形标记，途经点和终点的名称合并到距离/时间标签中，标签放在每段的终点位置
- **路线覆盖物注册时机**：`AMap.Driving`是异步的，应在创建停靠点标记后立即将它们注册到`dayOverlays`，不要等到Driving回调中才注册
- **信息窗中的链接**：POI的InfoWindow应包含可点击的来源帖子链接（`<a>`标签）
- **控制面板行程链**：每天的景点列表下方增加`A → B → C`格式的行程链，每个站点可点击跳转
- **路线颜色区分**：不同天的路线用不同颜色，在控制面板中用对应颜色的色块标识

### 17. 地图HTML中跨城路线的处理经验
- 当行程涉及两个相距较远的城市，地图需要支持城市切换功能
- 添加城市快速跳转按钮，点击后自动缩放到对应城市的视图
- 初始视图聚焦在主要停留城市，另一城市通过按钮或Day切换时跳转
- 路线缓存key应加上城市前缀，避免与其他攻略项目的路线缓存冲突
- 核心景点之间距离很近的独立景区，驾车路线API可能返回极短路线，此时直线距离与实际驾车距离差异不大

### 18. 小红书视频帖子判断经验
- 很多搜索结果看似图片攻略帖，实际是视频帖，下载后只得到mp4文件
- 如果帖子的content字段只有几个tag且likes数较高，很可能是视频帖
- 对于视频帖，如果文字content已有足够信息则不必下载；文字太少则寻找同一景点的纯图文帖子替代

### 19. 美食推荐交叉验证经验
- 被多个不同博主推荐的同一餐厅可信度较高
- 注意"网红店"现象：被大量推荐不代表好吃，可能只是知名度高
- 美食避雷帖的参考价值很高，尤其是有具体描述的，不能只看推荐帖
- 服务类投诉（上菜慢、态度差）在某些类型餐厅的帖子中非常常见，需要特别标注

### 20. 小红书美食帖子内容获取策略
- 很多美食推荐帖的文字content只有tag，实际推荐内容全在图片中（菜单、价格等）
- 部分美食帖图片主要是食物照片而非文字攻略，下载图片分析的价值有限
- 更有效的做法：优先阅读文字内容丰富的帖子，图片为主帖仅作补充
- 美食推荐交叉验证：被3个以上不同博主推荐的餐厅可信度高，但要注意区分网红店

### 21. 景区预约政策变动频繁的处理
- 热门景区的预约政策可能多次调整，搜索时务必注意帖子发布时间，优先采用最近6个月内的预约政策帖
- 导游类账号发布的政策信息通常较及时准确
- 自驾前往某些景区可能需提前办理通行证，注意提前量
